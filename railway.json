{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "apt-get update && apt-get install -y python3-pip && cd troubleshoot-assistant && pip3 install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "python3 troubleshoot-assistant/app.py",
    "restartPolicyType": "ON_FAILURE"
  }
}
