{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd troubleshoot-assistant && apt-get install -y python3-pip && pip3 install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "python3 troubleshoot-assistant/app.py",
    "restartPolicyType": "ON_FAILURE"
  }
}
